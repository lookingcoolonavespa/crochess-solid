export const NavBar = () => {
  // left hand side will have logo that leads back to home page
  // right hand side will have settings button
  //

  return (
    <nav>
      <div>croChess</div>
    </nav>
  );
};
